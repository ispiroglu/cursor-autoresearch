/** Limits for experiment output sent to the LLM. */
export const EXPERIMENT_MAX_LINES = 10;
export const EXPERIMENT_MAX_BYTES = 4 * 1024;

/** Rolling buffer / display truncation (aligned with pi-coding-agent defaults). */
export const DEFAULT_MAX_LINES = 500;
export const DEFAULT_MAX_BYTES = 256 * 1024;

export const METRIC_LINE_PREFIX = "METRIC";
